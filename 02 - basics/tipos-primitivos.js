let nome = 'Giovana'; //String Literal
let idade = 21; //Number Literal
let estaAprovado = true; //Boolean Literal (ture/false)
let segundoNome = undefined; //Quando não definimos um valor por padrão é undefined, nesse caso está apenas explicitado
let corSelecionada = null; //Usamos null em situações que queremos limpar o valor de uma variável